https://leetcode.com/problems/reverse-linked-list/

class Solution {
public:
    ListNode* reverseList(ListNode* head) {
        ListNode* newHead=NULL;
        while(head!=NULL){
            ListNode* nex=head->next;
            head->next=newHead;
            newHead=head;
            head=nex;
        }
        return newHead;
    }
};
SC : O(1)
TC : O(N) where n is the size of the linked list starting from the head 
