https://practice.geeksforgeeks.org/problems/reverse-a-doubly-linked-list/1

Node* reverseDLL(Node * head)
{
    //Your code here
    Node* temp =head;
    Node* ne=NULL;
    while(temp)
    {
        ne=temp->next;
        temp->next=temp->prev;
        temp->prev=ne;
        head=temp;
        temp=temp->prev;
    }
    return head;
}

SC : O(1)
TC : O(N)
